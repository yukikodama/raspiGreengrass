'use strict';

var test = require('unit.js');
var assert = require("assert");

describe('Dummy Test.', function() {
    it('verifies dummy response', function(done) {
        assert(true);
        done();
    });
});